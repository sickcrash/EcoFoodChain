// Intentionally left minimal.
// Remove custom re-declarations of external libraries to avoid conflicts
// with official type definitions provided by installed packages.

// If you need app-specific globals, declare them here, e.g.:
// declare global {
//   var resetAuthState: (() => void) | undefined;
//   var handleJwtExpired: (() => Promise<void>) | undefined;
// }
